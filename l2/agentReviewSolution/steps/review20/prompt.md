<!-- modelType: reasoning; output: review20.schema.json; private staging only -->

Compare the candidate with its base and the user's persisted request. Return one structured response only. You may replace an existing scalar leaf. Use `basis: "base-unchanged"` only when the current value still equals the base. A field already edited in the candidate is protected: replace it only with `basis: "refine-candidate-edit"`, the exact current `expectedJson`, and a reason explaining why that explicit edit must be refined. Preserve IDs, schema versions, module identity, all other fields and all other modules. If the request is contradictory, ambiguous, or needs an addition/removal, return `decision: "clarification"`, no operations, and a legible question. This response is only a proposal: it is not an L4 validation or approval.
