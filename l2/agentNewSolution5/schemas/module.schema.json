{
  "$id": "https://collab.codes/schemas/agentNewSolution5/module/2026-09-10-ns5-module-v2",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "requestKind",
    "schemaVersion",
    "moduleName",
    "title",
    "userLanguage",
    "productLanguages",
    "defaultLanguage",
    "sourcePrompt",
    "actors"
  ],
  "properties": {
    "requestKind": {
      "description": "Intent gate read by steps/module10/agentNs5Module.ts before anything is written. It is not persisted on module.defs.ts.",
      "type": "string",
      "enum": ["moduleRequest", "notAModuleRequest"]
    },
    "schemaVersion": { "type": "string", "const": "2026-09-10-ns5-module-v2" },
    "moduleName": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
    "title": { "type": "string", "minLength": 1 },
    "userLanguage": { "type": "string", "minLength": 2 },
    "productLanguages": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 2 }
    },
    "defaultLanguage": { "type": "string", "minLength": 2 },
    "sourcePrompt": { "type": "string", "minLength": 1 },
    "actors": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/actor" }
    }
  },
  "$defs": {
    "actor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actorId", "kind", "origin", "title", "description"],
      "properties": {
        "actorId": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "kind": { "type": "string", "enum": ["internal", "external", "system"] },
        "origin": { "type": "string", "enum": ["named", "inferred"] },
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 }
      }
    }
  }
}
