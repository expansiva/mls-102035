{
  "$id": "https://collab.codes/schemas/agentNewSolution5/ontology-entity/2026-09-11-ns5-ontology-v2",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["entityId", "fields", "lifecycleStates", "transitions"],
  "properties": {
    "entityId": { "type": "string", "pattern": "^[A-Z][A-Za-z0-9]*$" },
    "fields": {
      "type": "array",
      "items": { "$ref": "#/$defs/field" }
    },
    "uniqueKeys": {
      "type": "array",
      "items": {
        "type": "array",
        "minItems": 2,
        "items": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" }
      }
    },
    "details": {
      "type": "array",
      "items": { "$ref": "#/$defs/detail" }
    },
    "maintenance": { "type": "string", "enum": ["crud"] },
    "lifecycleStates": {
      "type": "array",
      "items": { "$ref": "#/$defs/lifecycleState" }
    },
    "transitions": {
      "type": "array",
      "items": { "$ref": "#/$defs/transition" }
    }
  },
  "$defs": {
    "fieldType": {
      "type": "string",
      "enum": ["uuid", "string", "text", "number", "integer", "boolean", "money", "date", "datetime", "json"]
    },
    "field": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fieldId", "title", "type", "required", "description"],
      "properties": {
        "fieldId": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "title": { "type": "string", "minLength": 1 },
        "type": { "$ref": "#/$defs/fieldType" },
        "required": { "type": "boolean" },
        "unique": { "type": "boolean" },
        "enum": {
          "type": "array",
          "items": { "$ref": "#/$defs/enumValue" }
        },
        "constraints": { "$ref": "#/$defs/constraints" },
        "description": { "type": "string", "minLength": 1 }
      }
    },
    "enumValue": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value", "title"],
      "properties": {
        "value": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "title": { "type": "string", "minLength": 1 }
      }
    },
    "constraints": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "maxLength": { "type": "integer", "minimum": 1 },
        "precision": { "type": "integer", "minimum": 0 }
      }
    },
    "detail": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type", "description"],
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "type": { "$ref": "#/$defs/fieldType" },
        "description": { "type": "string", "minLength": 1 }
      }
    },
    "lifecycleState": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "reachedBy"],
      "properties": {
        "state": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "reachedBy": { "type": "string", "enum": ["actor", "command", "time"] }
      }
    },
    "transition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["transitionId", "from", "to", "by", "description"],
      "properties": {
        "transitionId": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "from": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" }
        },
        "to": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "by": { "$ref": "#/$defs/transitionBy" },
        "description": { "type": "string", "minLength": 1 },
        "ruleRefs": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" }
        }
      }
    },
    "transitionBy": {
      "oneOf": [
        { "type": "string", "enum": ["system", "time"] },
        {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" }
        }
      ]
    }
  }
}
