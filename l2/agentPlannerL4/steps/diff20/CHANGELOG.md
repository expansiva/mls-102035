# diff20

## 2026-09-21 (p4_09)

- New deterministic step between `entry10` and `dispatch20`.
- Writes `pool/l1/web/l4diff.json` and `pool/l2/web/l4diff.json` (base sealed
  release × `/candidate` root). No candidate ⇒ `items: []`.
